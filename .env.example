# GreenShift — environment variables for local development
# Copy to .env and fill in your values
# NEVER commit .env to Git

# ─── Carbon API ───────────────────────────────────────────────
ELECTRICITY_MAPS_API_KEY=
CARBON_API_BASE_URL=https://api.electricitymap.org/v3

# ─── Tariff API ───────────────────────────────────────────────
TARIFF_API_KEY=
TARIFF_API_BASE_URL=https://api.example-tariff.com/v1

# ─── Database ─────────────────────────────────────────────────
# SQLite (local dev):
DATABASE_URL=sqlite:///./greenshift.db
# PostgreSQL (production):
# DATABASE_URL=postgresql://greenshift:password@postgres:5432/greenshift

# ─── Kubernetes ───────────────────────────────────────────────
# KUBECONFIG is auto-detected from ~/.kube/config
# K8S_NAMESPACE=greenshift

# ─── Application ──────────────────────────────────────────────
LOG_LEVEL=INFO
API_HOST=0.0.0.0
API_PORT=8000
ENVIRONMENT=development

# ─── Cache ────────────────────────────────────────────────────
CACHE_TTL_SECONDS=300

# ─── CSV Data Sources (highest priority — overrides API and mock) ──────────
# Set these paths to use real carbon/tariff CSV data.
# See docs/CSV_DATA_FORMAT.md for the expected format.
CARBON_CSV_PATH=
TARIFF_CSV_PATH=D:\electricity api key\electri.csv

# INR to USD conversion rate (used when tariff CSV prices are in INR)
TARIFF_INR_TO_USD=0.012

# ─── Dispatcher ───────────────────────────────────────────────
DISPATCHER_POLL_INTERVAL_SECONDS=30
