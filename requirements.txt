# GreenShift Python Dependencies

# Core framework
fastapi==0.115.6
uvicorn[standard]==0.32.1
pydantic==2.10.6
pydantic-settings==2.2.1

# Database
sqlalchemy==2.0.30
alembic==1.13.1

# SQLite driver (built-in, no extra package)
# PostgreSQL driver (for production / Kubernetes PostgreSQL deployment)
psycopg2-binary==2.9.9

# HTTP clients
httpx==0.27.0
requests==2.31.0

# Kubernetes
kubernetes==29.0.0

# Dashboard
streamlit==1.40.0
pandas==2.2.2
plotly==5.22.0

# Environment / config
python-dotenv==1.0.1

# Async support
anyio==4.3.0

# Hashing / crypto (stdlib, no package needed for SHA-256)

# Scheduling
apscheduler==3.10.4

# Testing
pytest==8.2.0
pytest-asyncio==0.23.6
pytest-cov==5.0.0
# httpx already listed above — used both as HTTP client and async test client

# Utilities
python-dateutil==2.9.0
pytz==2024.1
tenacity==8.3.0      # retry logic for API calls
cachetools==5.3.3    # in-memory caching for API responses
